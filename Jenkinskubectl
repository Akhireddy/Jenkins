pipeline {
    agent { label 'WORKSTATION' }
    stages {
      stage('check nodes') {
        steps {
          sh 'kubectl get nodes'
          }
      }   
       stage('create pod') {
         steps {
           sh 'kubectl run --name httpd --image=httpd'
           }
       }   
     }   
  }  
