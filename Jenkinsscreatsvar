pipeline {
  agent any
  environment {
    URL1 = "google.com"
    SSH = credentials("akhigcp")
  }
  stages {
    stage('ONE') {
      steps {
        sh 'echo ${URL1}'
        sh 'env'
        echo SSH
      }
    }
  }
}
